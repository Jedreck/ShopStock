<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jedreck.shopstock">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".OUTPart.OutActivity"
            android:label="@string/title_activity_out"
            tools:ignore="InnerclassSeparator"
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".BarCodeActivity.CaptureActivity"
            tools:ignore="InnerclassSeparator"
            android:launchMode="singleTask">
        </activity>
            android:name=".BarCodeActivity.CaptureActivity"
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name=".BarCodeActivity.ResultActivity"
            tools:ignore="InnerclassSeparator"
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".BarCodeActivity.TestScanActivity"
            tools:ignore="InnerclassSeparator"
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".MajorSearch.MainActivity"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask">
            android:label="@string/title_activity_main"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FullInfoActivity.FullInfoActivity"
            android:theme="@style/Theme.Design.NoActionBar"
            tools:ignore="InnerclassSeparator"
            android:launchMode="singleTask"/>
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name=".Store.Storeyes"
            android:label="@string/title_activity_storeyes"></activity>
        <activity
            android:name=".Store.Storeno"
            android:label="@string/title_activity_storeno">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
    </application>

</manifest>